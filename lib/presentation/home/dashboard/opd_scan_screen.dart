import 'package:flutter/material.dart';
class OpdScanScreen extends StatelessWidget {
  const OpdScanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('OPD Scan')),
      body: const Center(child: Text('OPD Scan Screen')),
    );
  }
}